# VibraPilot v1.0.6.14 Managed Persistent Browser + Closed Task Recovery Verification

## Baseline

- Official previous baseline: VibraPilot v1.0.6.13
- GitHub baseline commit: `5f082df8d1226710c095d4a8e591fb153c02c1c3`
- Target: v1.0.6.14
- Scope: `VP-MANAGED-PERSISTENT-BROWSER-001` + amended `VP-CLOSED-TASK-RECOVERY-001`

## Runtime surface

Only the approved runtime/config files may change:

- `src/vibrapilot/backend.py`
- `src/vibrapilot/qt_app.py`
- `src/vibrapilot/task_runtime_store.py`
- `config/settings.defaults.json`

## Verification requirements

- repository verifier PASS
- full pytest PASS
- full unittest PASS
- v1.0.6.14 targeted scope tests PASS
- SQLite schema version remains 1
- no new tables/columns/dependencies/pages
- Phase-01 lifecycle tests stay green
- licensing/selectors/workflow/reporting/Workflow Inputs frozen checks stay green
- replace-ready delta overlay has zero missing/extra/mismatched distributable files

## Windows live gate still required

Before baseline freeze, verify with real Google Chrome:

1. Task 1 profile persists cookie/localStorage/history after Close Browser and reopen.
2. App restart with the same Task slot reuses the same managed profile.
3. Task 1 and Task 2 profile data/history remain isolated.
4. The user's normal Chrome `User Data` path is blocked.
5. Manual Chrome close still produces the verified Phase-01 Closed/Not Verified state.
6. Forced internal persistent recycle does not create a false external Closed state and login is safely re-verified.
7. Close Task archives the exact Task state; Open Closed Tasks restores same slot/run/items/progress/result continuity with browser Closed.
8. Closed Tasks survive application restart and can be reopened without auto-browser, auto-workflow or auto-Send.
9. Zero traceback/runtime error.
10. GitHub Actions is green.

## Source-level verification result

Final exact-overlay verification from the v1.0.6.13 baseline reports:

- `compileall`: PASS
- repository verifier: PASS
- pytest: **191 passed, 5 skipped, 134 subtests passed**
- unittest: **154 tests OK, 5 skipped**
- v1.0.6.14 targeted tests: **15 passed**
- exact delta overlay: **0 missing / 0 extra / 0 mismatched distributable-development files**
- clean public source archive verification: PASS

The local skips are platform/private-source dependent and are not treated as Windows live acceptance.

## Candidate verdict

**SOURCE-VERIFIED / WINDOWS LIVE GATE PENDING.** v1.0.6.14 must not be declared the next Official Baseline until real Windows/Chrome persistence, history/isolation, Closed Task recovery and GitHub CI pass.
