# VibraPilot v1.0.6.36 Share Invite Externalization Verification

Baseline: `VibraPilot_v1.0.6.35_BASELINE.zip`  
Baseline commit: `c5511a82ddf164bfacdfad5aa12ebf75ad56a1da`  
Baseline SHA-256: `c5be0d2221ee580360c32066a29462cba75eb330b8e408bb9dcea780e6d9f229`
Standalone Share Invite SHA-256: `6fe7f95bdf8bce5f9e22cfb2d375bafb8d5af857af5b9d1460c3cb5d372e1c50`

## Required release invariants

- Source-controlled built-in workflow registry is empty.
- `src/vibrapilot/workflow/share_invite/` is absent from the Core release.
- Workflow-state schema is v2 and `active_workflow_id = null` is valid.
- Schema-v1 `share_invite` active state migrates without quarantine or value loss.
- First activation from `None` uses the normal atomic commit path.
- Core backend contains no `ShareInviteWorkflow` import/type branch.
- Share Invite selectors, runtime methods and specialized `process_item` orchestration match frozen v1.0.6.35 after the external-host adapter normalization.
- Plugin API remains 1; legacy `load_task_items` behavior remains available.
- DMARC v1.0.1 can coexist with the standalone Share Invite package in one plugin-only catalog.
- Browser, licensing, TaskRuntime/workspace schema, dependencies, CI and build surfaces remain frozen.

## Verification commands

```text
py scripts\verify_repository.py
py -m pytest -q
py -m unittest discover -s tests -q
py scripts\diagnostics\verify_v10636_share_invite_externalization.py
py scripts\diagnostics\verify_share_invite_workflow_v10.py Share_Invite_v1.0.vpworkflow VibraPilot_v1.0.6.35_BASELINE.zip
```

Windows live acceptance remains required before Git publication.
