# VibraPilot v1.0.6.12 Browser UI/Lifecycle Verification

## Scope

Plan: `VP-BROWSER-UI-LIFECYCLE-001`

Baseline archive SHA-256: `9ecb7cd66f24832c3555d219a6f8aaf47358877dd417eeb703b5a755964fc90a`

GitHub baseline commit: `8670415b1df221ebeeb7d8f3fba4f991a91d43ec`

Target: `1.0.6.12`

## Verified source invariants

- runtime change surface limited to `backend.py` and `qt_app.py` approved methods,
- Browser Settings defaults unchanged,
- TaskRuntimeStore schema/module unchanged,
- licensing modules/config unchanged,
- Workflow Inputs unchanged,
- design/focus files unchanged,
- site-specific selectors/Send methods frozen by method-level AST scope locks,
- no new dependency.

## Browser lifecycle regression coverage

Automated tests cover:

- owner-thread readiness requires a live active page and connected non-persistent browser,
- UI readiness consumes only a thread-safe event,
- manual active-page close,
- replacement-page behavior with login re-verification,
- context close,
- browser disconnect,
- idempotent lifecycle event registration,
- four-state browser action contract,
- preserved persistent-profile defaults,
- workspace safe-screen geometry markers.

## Final source-level verification

Working-tree verification before packaging:

- `python -m compileall -q .` — PASS
- `python scripts/verify_repository.py` — PASS
- `python -m pytest -q` — **172 passed, 5 skipped, 138 subtests passed**
- `python -m unittest discover -s tests -p "test_*.py" -q` — **135 tests OK, 5 skipped**

The five skips are expected in this non-Windows forensic environment where PySide6 lifecycle tests are not available. Windows acceptance must execute those runtime tests rather than treating the skips as production acceptance.

## Windows acceptance still required

Before Official Baseline Freeze:

1. Open Browser → Open state → Close Browser → Closed state → reopen.
2. Manually close Chrome/window and verify Task, Login and Dashboard synchronize.
3. Repeat with two independent Tasks.
4. Verify controlled browser close preserves loaded data/counts.
5. Verify Activation → Workspace opens fully within the current monitor.
6. Require zero traceback/runtime error.
