# v1.0.6.28 PR-11 Windows / Multi-Task Regression Verification

Plan: `VP-PR11-WINDOWS-MULTITASK-E2E-001`.

The PR-11 verifier freezes every `src/vibrapilot/**` file to the v1.0.6.27 baseline and permits only verification tooling/tests/version/docs. Target-Windows evidence uses `PASS`, `FAIL`, `BLOCKED`, `NOT_RUN`, or `OWNER_ACCEPTED_RESIDUAL`; residual status requires explicit owner acceptance.

A reproducible supported-path product defect stops PR-11 and requires a separate owner-approved production scope amendment. No automatic runtime fix is authorized.
