# v1.0.6.38 Portable Runtime Root Fix — Forensic Verification

## Authoritative evidence

- Input source commit: `299e93a89db3d30505350f474f79eefc330ee923`
- User-supplied snapshot: `Updated_Baseline.zip`
- Snapshot SHA-256: `007d339193e7f02bc316514e82512174034d611c889a327580f59a32a24bfddb`
- Failed portable run: `32056816056`
- Failed job: `95468779983`
- Startup diagnostics artifact: `9297920798`
- Diagnostics artifact SHA-256: `4e26c5b3f9683cd3422e178d1cd7472ac8057286f6bf683880aca3b772fe9ec0`

## What passed before startup

- Windows Server 2022 build host
- Python 3.12 x64
- source verifier + full pytest + unittest
- Nuitka 4.1.3 standalone OneDir compilation
- Playwright control-driver inclusion
- explicit browser inclusion `none`
- portable directory and ZIP creation
- per-file and ZIP SHA verification
- no bundled Chrome/Chromium
- no WiX/MSI
- Qt platform DLL presence

## Exact failure

Captured stderr:

- `FileNotFoundError` for `...\release\config\settings.defaults.json`
- wrapped as `RuntimeError: Settings defaults could not be loaded`

The verified payload contained `config/settings.defaults.json` under `...\release\VibraPilot-1.0.6.37-Windows-x64-Portable\`. This proves a one-directory-high root calculation.

## Corrective scope

Production change: `src/vibrapilot/runtime_environment.py` only.

Nuitka compiled root now equals the launched executable directory. PyInstaller and source-mode contracts are regression-tested unchanged. Frozen hashes lock backend, Qt UI, TaskRuntimeStore, Chrome runtime/installer/Authenticode, general CI, portable workflow and dependencies.

## Baseline snapshot hygiene

The user-supplied ZIP is accepted as the authoritative input snapshot, but it is a workspace snapshot rather than a release-grade clean source archive: it contains `.git`, private `project/`, runtime AppData/Logs/Reports/FailedData, test caches, and stray command-output placeholders (`-D`, `-n`, `RUN_ID`). These paths are not copied into the v1.0.6.38 Delta and must not be pushed publicly.

## Final freeze gate

v1.0.6.38 is not declared final solely from source tests. Final baseline freeze requires PR CI, post-merge main CI, a fresh GitHub `workflow_dispatch` portable build with green startup smoke, owner Windows live acceptance, and a clean source-only baseline ZIP with SHA-256.
