# v1.0.6.39 Runtime Reliability & Workflow-Scoped Session Policy — Verification

## Scope identity

`VP-V10639-RUNTIME-RELIABILITY-SESSION-POLICY-001`

Official baseline: v1.0.6.38 / `bc894115f505b7b9ecbc15a235b91d37a9693cec`.

## Automated gates

The candidate must pass:

- Phase 1 targeted tests;
- historical browser/workflow/recovery regression tests;
- full `python -m pytest -q`;
- full `python -m unittest discover -s tests -q`;
- `python -m compileall -q src config scripts tests`;
- `python scripts/verify_repository.py`;
- scope diff and frozen-file hash verification;
- clean delta apply verification.

## Windows owner acceptance still required

Source tests cannot substitute for these live Windows gates:

1. 3 and 4 concurrent Tasks make progress with one browser foreground and the others minimized/occluded.
2. VibraPilot can be minimized while Tasks continue.
3. The operator can work in another application without focus-loss pausing Tasks.
4. A Windows system power request is present while processing and is released after the last Task ends/stops/fails.
5. Session-required and sessionless workflow behavior both match their Task schema.
6. Multi-tab login/preparation, popup ownership and active-page closure behave deterministically.

Until those live gates pass, the candidate is **LOCAL VERIFIED / WINDOWS ACCEPTANCE PENDING**, not the final frozen release.
