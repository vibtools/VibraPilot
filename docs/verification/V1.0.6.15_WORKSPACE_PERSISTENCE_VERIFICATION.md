# VibraPilot v1.0.6.15 Workspace Persistence Verification

## Verification target

`VP-WORKSPACE-PERSISTENCE-001` from the verified v1.0.6.14 baseline.

## Required checks

- Scope contract matches only `src/vibrapilot/qt_app.py` plus new `src/vibrapilot/workspace_state.py` at runtime.
- `TaskRuntimeStore.SCHEMA_VERSION == 1` and baseline backend/task-runtime/settings/browser-profile files remain frozen.
- `WorkspaceStateStore` round-trip, atomic replacement, corrupt/unsupported-state quarantine and metadata-only serialization pass.
- Active Task restoration preserves slot/order/Target URL and reuses existing SQLite run data where present.
- Deliberately Closed Tasks are not automatically restored.
- Restored browsers are Closed, Login is Not Verified and no workflow/Send starts.
- Workspace-restored run IDs are not offered twice by crash recovery.
- Selected page and window geometry restore safely; off-screen geometry clamps to an available monitor.
- Full pytest, unittest, Windows live runtime and GitHub Actions must be green before baseline freeze.
