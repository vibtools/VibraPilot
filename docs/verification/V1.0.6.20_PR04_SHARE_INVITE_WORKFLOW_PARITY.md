# VibraPilot v1.0.6.20 PR-04 Share Invite Workflow Parity Verification

## Scope

Verification contract: `config/verification/v1.0.6.20_pr04_share_invite_workflow_extraction_scope.json`.

PR-04 is an extraction/parity phase. It must not redesign the safety-sensitive Share Invite processing state machine.

## Verified invariants

- Share Invite manifest and copied logo are deterministic and source-controlled.
- Built-in registry exposes `share_invite`; default manager remains empty.
- No external plugin discovery or manifest-controlled arbitrary Python import exists.
- Share Invite selector lists and matching priority equal the approved baseline.
- Existing backend exception identities are passed into the workflow rather than redefined.
- `process_item`, `_register_send_click_attempt`, `process_batch`, `report_row` and login refresh safety paths retain approved canonical AST fingerprints.
- Invalid/blank email is rejected before browser interaction.
- Send re-verifies the exact e-mail value and preserves the pre-click durable marker callback.
- Existing Test Send Limit exception identity is preserved.
- Frozen UI, settings, browser, persistence and licensing files retain baseline hashes.

## Automated verification

Pre-version implementation gate on the development audit host:

```text
PR-03 + PR-04 targeted pytest: 43 passed
compileall: PASS
scripts/verify_repository.py: PASS
full pytest: 273 passed, 5 skipped, 115 subtests passed
full unittest: 189 OK, 5 skipped
```

The pytest subtest count changes from the historical 133 to 115 because 18 legacy `AutomationWorker` freeze subtests are deliberately superseded for the 18 methods explicitly approved for PR-04 delegation. Dedicated PR-04 parity tests and canonical AST/hash checks cover the approved extraction boundary; the normal pytest test count increases from 252 to 273. No failing test is hidden or reclassified.

The test host is not a replacement for the later required Windows x64 / Python 3.12 production runtime gate. Release publication and GitHub CI are separate mandatory PR-04 completion gates.
