# VibraPilot v1.0.6.8 — Workflow Inputs verification

## Baseline

`VP-WORKFLOW-INPUTS-001` is anchored to the final v1.0.6.7 source tree produced by the clean v1.0.6.7 Official Baseline plus the approved Windows SQLite concurrency correction. The canonical final-tree fingerprint is:

`84d22fd2c1fef38cbf49024f7d3b2c9ec250e3389dd2b479192735fb2419bb89`

## Runtime scope

Only these runtime-source paths are approved:

- `src/vibrapilot/qt_app.py`
- `src/vibrapilot/workflow_inputs.py` (new)

The machine-readable contract is `config/verification/v1.0.6.8_workflow_inputs_scope.json`.

## Verified contract

- Workflow Inputs is a real top-level page after Tasks.
- The four existing workflow keys are absent from App Settings and present through the dedicated page metadata.
- `default_target_url` remains in App Settings.
- Existing saved values remain compatible because the same settings keys and `SettingsManager` persistence are retained.
- Reset affects only the four workflow-input keys.
- No single-option/fake workflow selector is present.
- Existing View shortcuts for Dashboard, Tasks, Reports, Live Logs, App Settings and Browser Settings are preserved.
- Backend worker, selectors, task runtime store, Browser Settings and Licora API v2 are frozen by file/AST scope checks.

## Release verification

The release gate includes repository verification, pytest, standard-library unittest, compile checks, scope checks, archive hygiene and delta replacement simulation. Windows GUI launch and hosted GitHub CI remain platform gates after applying the delta to the final v1.0.6.7 working tree.
