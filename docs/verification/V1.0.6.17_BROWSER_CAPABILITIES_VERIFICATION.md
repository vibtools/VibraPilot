# VibraPilot v1.0.6.17 Browser Capabilities Verification

## Contract

Scope: `VP-BROWSER-CAPABILITIES-001`
Baseline: v1.0.6.16 / `fd0cbe6e8f3fc37f92bdf49396364ce74583fd1e` / Actions `31342562832`.

## Required source verification

- `config/settings.defaults.json` hash remains frozen.
- TaskRuntimeStore schema remains version 1.
- WorkspaceState schema remains unchanged.
- Licensing, Workflow Inputs, managed profiles, Closed Task recovery, workspace persistence and Qt focus runtime remain frozen.
- Runtime source changes are restricted to `backend.py`, `qt_app.py` and new `browser_capabilities.py`.

## Required Windows live checks

1. Download a controlled file and require a durable saved copy in the Task's effective directory.
2. Close browser/app and require the file to remain.
3. Download the same filename twice and require non-destructive suffixing.
4. Trigger single-file, multiple-file and directory chooser inputs; require a native user-selection dialog and correct page input.
5. Cancel a chooser and close a page/browser while a chooser is pending; require no traceback or stale cross-Task selection.
6. Validate one controlled unpacked MV3 extension using persistent compatible Chromium.
7. Require existing managed profile, workspace restoration, Closed Task and Razorpay/Test Mode behaviors unchanged.

## CI gate

Run repository verification, full pytest, full unittest and `tests/test_v10617_browser_capabilities.py`; require the new GitHub Actions run green before v1.0.6.17 baseline freeze.
## Source-level verification result

- Repository verifier: **PASS**.
- Full pytest: **213 passed, 5 skipped, 133 subtests passed**.
- Standard-library unittest: **176 tests OK, 5 skipped**.
- Dedicated `tests/test_v10617_browser_capabilities.py`: **9 passed**.
- Exact frozen hashes for settings, TaskRuntimeStore, WorkspaceState, licensing, Workflow Inputs and focus manager: **PASS**.
- Production runtime/config drift is restricted to `backend.py`, `qt_app.py` and new `browser_capabilities.py`.
- Windows live browser capability checks and the new GitHub Actions run remain required before v1.0.6.17 baseline freeze.

