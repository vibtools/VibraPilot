# VibraPilot v1.0.6.33 Browser Forensic Closure Verification

## Frozen input

- Version: v1.0.6.32
- Git commit: `a001f67972c47832a5e59af5f9350a0409e7eab6`
- Clean source archive SHA-256: `fdc18905084f41f9418d239f5e8f0ab632fa114c05b065835dcc126d32a1664f`
- Uploaded archive hygiene: no `.git`, AppData, Logs, Reports, FailedData, private `project/`, `__pycache__`, or `.pytest_cache` entries.

## Baseline verification

The untouched uploaded baseline passed repository verification, compileall, pytest, and unittest in the audit environment. Windows GitHub CI for v1.0.6.32 independently passed `436 passed, 1 skipped, 113 subtests` and `200 OK, 1 skipped` on CPython 3.12.10.

## Finding register

| ID | Severity | Finding | Closure |
|---|---|---|---|
| F-001 | P1 | Runtime preflight could validate a different Chrome executable from Playwright's channel target | Playwright channel-order resolution mirrored; first existing target is authoritative and fail-closed |
| F-002 | P1 | Installed Chrome identity relied on ProductName only | WinVerifyTrust + Google LLC signer required |
| F-003 | P1 | Same-host MSI URL path accepted arbitrary prefix ending in approved filename | Exact path required |
| F-004 | P2 | MSI exit 1602 classified as generic failure | Distinct `installer_cancelled` result |
| F-005 | P2 | Security/lifecycle progress events could be dropped under UI queue saturation | Only download byte progress is droppable |
| F-006 | P2 | Concurrent prerequisite UI could reset active install controls | Active install state preserved; re-check guarded |
| F-007 | P2 | Diagnostics could claim compliance from path shape/requested channel rather than trusted measured identity | Measured process path must equal trusted runtime; otherwise violation/unverified |
| F-008 | P3 | Public docs still described v1.0.6.32 as candidate after main promotion | Docs synchronized for v1.0.6.33 closure |
| F-009 | P3 | Extra EOF blank line in `chrome_runtime.py` | Source hygiene corrected |

No CAPTCHA bypass, stealth, fingerprint spoofing, new workflow behavior, licensing change, dependency change, database change, or build/package change is part of this hotfix.

## Owner Windows acceptance required

After applying the delta, Windows acceptance must confirm:

1. diagnostic reports trusted Google Chrome with signer `Google LLC`;
2. measured browser executable equals trusted Chrome executable;
3. `engine=google_chrome`, `fallback=no`, `sandbox=on`;
4. existing Chrome Open → Close → Open remains healthy;
5. on a safe missing-Chrome environment, cancellation and successful installer flows behave as documented.
