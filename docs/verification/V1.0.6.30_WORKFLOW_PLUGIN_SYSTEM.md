# v1.0.6.30 Workflow Plugin System Verification

Baseline: v1.0.6.28 commit `fff8160157d4d9b68b2d28b11105b0f7f38ed17d`.

Verification requires: approved-scope-only production changes; trusted package inspect/install/catalog validation; broken/incompatible plugin fail-closed behavior; per-workflow Input/Settings isolation; per-Task configuration persistence; Core Status plus Workflow Step/metrics wiring; existing Share Invite and PR05–PR11 regression; full pytest/unittest/repository verification; no private `project/`, PR-12 packaging, marketplace, sandbox or per-Task mixed-workflow implementation in the delta.

## Post-apply regression repairs

`V10630-QT-TASKSLOT-FAKEAPP-COMPAT-001` was reproduced from Windows full-test evidence. Cycle-1 restored the historical lightweight Qt TaskSlot host contract without changing production MainWindow workflow behavior; owner Windows verification subsequently passed with 406 pytest tests / 1 skipped and 200 unittest tests / 1 skipped.

`V10630-MAINWINDOW-STATICMETHOD-STARTUP-001` was reproduced by `py run.py` on the pushed candidate `e0a080062a4ddb783dc94568801358ce2e01598c`. The v1.0.6.30 edit had dropped the baseline `@staticmethod` decorator from `MainWindow._transaction_root_has_directories(root)`. Because `_workflow_recovery_block_reason()` invokes that helper through `self`, Python injected an implicit `self` plus the explicit path and raised `TypeError` during Workflows-page registration. The workspace therefore stopped while partially built, which also explained the blank Tasks area/UI appearance.

The repair restores the exact descriptor only, adds a generic source-level bound-method descriptor regression check, and extends the post-apply verifier with the same contract. Global UI design tokens and frozen browser/Task/workflow-runtime surfaces remain unchanged. Clean-candidate verification after the repair: compileall PASS, targeted affected matrix 40 passed, full pytest 403 passed / 5 skipped / 113 subtests, full unittest 200 OK / 5 skipped. Final Windows `py run.py` startup acceptance is required after applying this repair.
