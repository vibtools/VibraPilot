# v1.0.6.30 Workflow Plugin System Verification

Baseline: v1.0.6.28 commit `fff8160157d4d9b68b2d28b11105b0f7f38ed17d`.

Verification requires: approved-scope-only production changes; trusted package inspect/install/catalog validation; broken/incompatible plugin fail-closed behavior; per-workflow Input/Settings isolation; per-Task configuration persistence; Core Status plus Workflow Step/metrics wiring; existing Share Invite and PR05–PR11 regression; full pytest/unittest/repository verification; no private `project/`, PR-12 packaging, marketplace, sandbox or per-Task mixed-workflow implementation in the delta.
## Post-apply regression repair

`V10630-QT-TASKSLOT-FAKEAPP-COMPAT-001` was reproduced from Windows full-test evidence: the v1.0.6.30 `TaskSlotWidget` constructor assumed MainWindow-only workflow attributes and broke the frozen lightweight Qt lifecycle-test host. Cycle-1 restores constructor compatibility with an inert Share Invite schema only when those workflow host attributes are absent; production MainWindow continues through the full plugin catalog/state path. Final Linux/source verification after the repair: repository verifier PASS, pytest 402 passed / 5 skipped / 113 subtests, unittest 200 OK / 5 skipped. Windows PySide6 re-verification remains the owner-side acceptance gate.

