# v1.0.6.32 Chrome Prerequisite Secure Install — Verification

Baseline: `v1.0.6.31` / `fc9081b0f760ac6b380b8c574680fc2c15764be0`.

## Automated gates

- v1.0.6.32 scope contract validates the exact allowed production surface.
- Secure installer unit tests cover HTTPS/host policy, atomic download, cancellation, SHA-256, Authenticode rejection, Google LLC publisher enforcement, installer result handling and post-install re-detection.
- Phase-01 Chrome-only regression tests remain active.
- Repository verifier asserts no Chromium fallback, no build/dependency changes and no security-setting bypass.

## Required Windows owner acceptance

1. Chrome-present startup produces no prerequisite prompt and normal Chrome launch remains `fallback=no`, `sandbox=on`.
2. Controlled Chrome-missing state shows **Google Chrome Required** without preventing non-browser app use.
3. **Not Now** performs no download/install and browser creation stays blocked.
4. **Download & Install** retrieves only the official Google MSI, validates Authenticode + Google LLC and invokes Windows UAC.
5. UAC cancellation reports cancellation and remains retryable without Chromium fallback.
6. Successful install is followed by genuine Google Chrome re-detection before browser automation becomes available.
7. Open → manual Close → Open remains green with the managed `BrowserProfiles/slot_N` profile.

Build/package acceptance is explicitly deferred to a later separately approved track.
