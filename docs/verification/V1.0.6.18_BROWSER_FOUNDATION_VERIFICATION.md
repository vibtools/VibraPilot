# VibraPilot v1.0.6.18 Browser Foundation Verification

## Source gate
```cmd
python scripts/verify_repository.py
python -m pytest -q
python -m unittest discover -s tests -p "test_*.py" -v
python -m pytest -q tests/test_v10618_browser_foundation.py -vv
```

## Windows gate
Capture `Logs\BrowserDiagnostics\slot_N_latest.json`. Verify actual executable/version/command line, profile and fallback. Compare Sandbox-OFF vs existing Sandbox-ON setting with browser launch, manual close, crash, reopen, persistent session, download/upload and 1/2/4 Tasks. Perform same-machine normal Chrome vs VibraPilot CAPTCHA comparison without evasion. Validate cookies, LocalStorage, IndexedDB, history, download, upload and unpacked extension.

Blocked Windows tests are not PASS. Do not change the fresh Sandbox default until the full Sandbox-ON matrix passes.
