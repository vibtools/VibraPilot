# v1.0.6.31 Chrome-Only Runtime Foundation — Verification

## Required source gates

- v1.0.6.31 targeted Chrome-only tests pass.
- historical/current browser regression suites pass with explicit v1.0.6.31 supersession.
- repository verifier passes.
- full pytest passes.
- frozen workflow/licensing/persistence/build surfaces remain byte-identical.

## Required policy evidence

- launch target is Google Chrome (`channel="chrome"`).
- no `channel="chromium"` launch path exists.
- no Chrome-to-Chromium retry exists.
- no arbitrary executable launch authority exists.
- `chromium_sandbox=True`.
- HTTP cache default is enabled.
- managed persistent profiles remain in use.

## Windows owner acceptance still required

Run the source app on Windows with installed Google Chrome and confirm browser open/close, managed profile persistence, effective sandbox evidence, no fallback log, and download/upload smoke behavior. Chrome-missing install UX is Phase 2 and is not a Phase-1 acceptance item.

## Current automated evidence — 2026-08-14

- targeted v1.0.6.31 Chrome-only tests: **19 passed**.
- repository verifier: **PASS**.
- full pytest: **421 passed, 6 skipped, 113 subtests passed**.
- unittest compatibility: **200 tests OK, 6 skipped**.
- `compileall`: **PASS**.
- Phase-1 source diagnostic: **PASS**; Windows runtime acceptance **NOT RUN** in the non-Windows audit environment.
- frozen workflow/licensing/persistence/build hashes: **PASS**.

Formal Phase-1 completion remains pending owner Windows acceptance.
