# v1.0.6.18 — VP-BROWSER-FOUNDATION-STABILIZATION-001

Adds observational browser identity/fallback/sandbox/environment diagnostics. Successful launches write structured JSON under `Logs/BrowserDiagnostics/` and a compact existing Live Logs summary. Chrome-preferred launch, fallback policy, profiles, capabilities, persistence, licensing, Test Mode, Razorpay workflow and Task UI are preserved.

Sandbox source default remains unchanged because the approved Windows Sandbox-ON acceptance is blocked in the current environment. CAPTCHA and existing capability/lifecycle areas remain validation-only unless a real Windows failure is reproduced.
