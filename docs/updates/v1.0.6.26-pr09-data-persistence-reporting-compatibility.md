# v1.0.6.26 — PR-09 Workflow Data/Persistence/Reporting Compatibility

PR-09 is a production-runtime verification phase. It confirms that the existing one-active-workflow + clear-on-real-switch lifecycle prevents wrong-workflow Task/runtime/result recovery without adding workflow identity to SQLite or report schemas. TaskRuntimeStore remains schema version 1; current TXT/CSV/XLSX/XLS import behavior, TaskItem fields, live report columns, CSV/XLSX exports and spreadsheet-safety semantics remain unchanged.

Successful real workflow switches continue to clear live Task runtime, WAL/SHM, checkpoints, workspace Task references and live results while preserving exported Reports, FailedData, Logs and PR-08 canonical `AppData/workflow_inputs.json`. Production registry remains `share_invite` only.
